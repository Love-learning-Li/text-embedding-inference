# 代码文件功能说明

在进行代码修改时，请明确以下文件定位及操作规范：models/flash_qwen3_SingleSafetensor.py当前为模型实现文件，utils/flash_attn.py为flash_attn函数接口定义文件，models/pooling.py为池化策略接口定义文件，flash_qwen3.py为已废弃文件（无需关注）。在对进行修改时，需严格限定修改范围，仅关注和NPU相关的函数实现，确保不影响其他功能模块的正常运行。

# 现象

当前输入文本，通过测试当前模型(models/flash_qwen3_SingleSafetensor.py)和输出向量(Defalut model)之间的余弦相似度来检查模型实现是否正确。根据当前模型文件测试得到余弦相似度仅0.0849。


# 任务

当前tei框架调用Qwen3-Embedding-0.6B的模型时，Default模型是通过如下方式调用的:
```python
model = (
            AutoModel.from_pretrained(model_path, trust_remote_code=trust_remote)
            .to(dtype)
            .to(device)
            .eval()
        )
```

transformers/models/qwen3中的文件存储在/Default_Model目录中，该目录中的代码仅用作检查models/flash_qwen3_SingleSafetensor.py实现是否有误。

- 请针对当前的models/flash_qwen3_SingleSafetensor.py模型文件，通过增加logger在终端打印每一步的中间量，并针对Default模型设计中间量打印的思路。针对Default模型的策略不要修改/Default_Model目录下的代码。

- 重点的中间量例如: position_ids、input_ids、attn_mask等等

